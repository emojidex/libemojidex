language: cpp

before_install:
  - sudo apt-get update -qq

install:
  - sudo apt-get install openjdk-8-jdk libboost1.55-dev libboost-filesystem1.55-dev libboost-test1.55-dev swig

compiler:
  - gcc

before_script:
  - mkdir build
  - cd build
  - cmake ..
  - make

script: make test
